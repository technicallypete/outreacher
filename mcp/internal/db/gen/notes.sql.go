// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: notes.sql

package db

import (
	"context"
)

const createNote = `-- name: CreateNote :one
INSERT INTO app.notes (lead_id, content)
VALUES ($1, $2)
RETURNING id, lead_id, content, created_at
`

type CreateNoteParams struct {
	LeadID  int32  `json:"lead_id"`
	Content string `json:"content"`
}

func (q *Queries) CreateNote(ctx context.Context, arg CreateNoteParams) (AppNote, error) {
	row := q.db.QueryRow(ctx, createNote, arg.LeadID, arg.Content)
	var i AppNote
	err := row.Scan(
		&i.ID,
		&i.LeadID,
		&i.Content,
		&i.CreatedAt,
	)
	return i, err
}

const getNotesByLead = `-- name: GetNotesByLead :many
SELECT id, lead_id, content, created_at
FROM app.notes
WHERE lead_id = $1
ORDER BY created_at ASC
`

func (q *Queries) GetNotesByLead(ctx context.Context, leadID int32) ([]AppNote, error) {
	rows, err := q.db.Query(ctx, getNotesByLead, leadID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []AppNote
	for rows.Next() {
		var i AppNote
		if err := rows.Scan(
			&i.ID,
			&i.LeadID,
			&i.Content,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
